# URL del backend compartido con el e-commerce. Vacío = mismo origen (sirve
# cuando en producción el POS se sirve desde el mismo dominio; si el POS vive
# en un subdominio propio, completar con la URL del backend, ej:
# https://fenixelectricidadiluminacion.com
VITE_API_URL=http://localhost:3001
